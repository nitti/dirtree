# Changelog

Chronological development history of `dirtree`, oldest to newest — each stage, fix, and rework, along with what was and wasn't manually verified at the time. For a concise summary of the current implementation state, see the "Status" section in the top-level [`README.md`](../README.md).

The implementation matches `specs/SPEC.md`'s open-files-primary-view design: a running list of open file previews as the primary view, with the browser (§3), an open-files switcher (§2.3), and quick open plus jump to file (§4) as overlays reachable from it (§5.1 covers the resulting view model). This was rebuilt in seven stages (each landed as its own PR) from an earlier design where the browser was the primary screen, followed by a keybinding/terminology pass that split the original combined jump/fuzzy-picker overlay into the two single-purpose overlays described below and renamed "tree explorer" to "browser" throughout the spec, tests, and code:

1. Core open-files data layer: `internal/openfiles` (open/reuse/fail semantics, remove/reorder), `preview.Load`'s failed-vs-opened result type, and `spinner`'s minimum-display-duration-skip/badge-decision logic.
2. Browser overlay wiring: auto-opens on startup, full §3.4 navigation, Space/`a` open files through `internal/openfiles` with inline failure messaging, per §2.2.
3. Jump/fuzzy-picker overlay wiring from both entry points (superseded — see stage 8): reveal-in-tree and open-into-list, with the header legend and Enter/Space mapping flipping per entry point.
4. Open-files list overlay (`Tab`): list rendering, Enter/`x`/Shift-Up/Shift-Down/Escape (§2.3).
5. Primary preview view: real content rendering (gutter, syntax highlighting, wrapping), scrolling, goto-line, and per-entry state restoration (§2.1).
6. Browser's dual split/popup layout (§5.1), recomputed every frame: split view puts the browser pane on the left with a read-only preview pane on the right; popup view floats a bordered window over the unmodified, full-width preview.
7. System behaviors and a full manual verification pass (resize polling, escape timeout, live filesystem refresh, the spinner badge's full sequence including debug mode, open-failure messaging for binary/permission-denied files).
8. Split stage 3's combined jump/fuzzy-picker overlay into two single-purpose overlays per current `specs/SPEC.md` §4: **quick open** (`O`, toggle, reachable from the primary preview view, opens the selected match into the open-files list) and **jump to file** (`/`, reachable only from within the browser, reveals the selected match in the browser and replaces the browser view while active). `B` (browser) and `O` (quick open) are now both toggles — pressing the same key again closes the overlay it opened, the same as Escape.

The pure-logic layers (`internal/tree`, `internal/ignore`, `internal/index`, `internal/match`, `internal/preview`, `internal/openfiles`, `internal/spinner`, `internal/search`) have automated test coverage against every group in `specs/TESTING.md`. `internal/ui` is hand-verified in a real terminal (tmux): startup (originally the auto-opened browser, now the empty preview view — see the startup fix noted further below); navigation, expand/collapse; Space/`a` open actions including inline binary/permission-denied failure messages; `B`/`Tab`/`O`/`/`/Escape/`q` view switching, including `B` and `O` closing their own overlay as a toggle; quick open's single open action and jump to file's single reveal-in-browser action (including jump to file replacing and then correctly returning to the browser); the open-files overlay including Shift-Up/Shift-Down reordering; real preview rendering with scrolling, goto-line, and per-entry scroll-state restoration; live filesystem refresh (add/delete reflected in both the browser and the background index) while the app is running; and the spinner badge's full threshold/spinner/completion/fade sequence, confirmed both in normal builds (tiny directories correctly show nothing at all) and via `-tags spinnerdebug` (bypasses only the perceptibility threshold; the minimum-display-duration floor, completion message, and fade still run on their normal schedule). The periodic resize-poll ticker's behavior inside Zellij — the primary target multiplexer per `specs/SPEC.md` §6.2, which doesn't always deliver resize signals reliably — has also been confirmed working as expected in earlier stages and was not re-verified in stage 8 (the resize/ticker code itself is unchanged by the rename).

**Content search (spec §9)**, added after the stages above: a separate `s`-triggered overlay, reachable from both the browser and the primary preview view, that scans indexed files' content (not just paths) for a case-insensitive match — a plain substring by default, or a regular expression in regex mode (Ctrl+R toggles it; an invalid pattern shows an inline error instead of scanning) — running the scan in a cancelable background goroutine per keystroke/mode-toggle so typing never blocks on a large tree, with a threshold-suppressed animated spinner (mirroring the indexing badge) once a scan runs long enough to be worth showing. The query lives on its own row beneath the header, with a background distinct from the results list so it's unmistakable at a glance. Results render as a two-level, expandable list — one row per matching file (all expanded by default) disclosing every matching line within it, not just the first — with Left/Right to collapse/expand a file's hits, Return to open the selected row and jump straight to its line (leaving the overlay open, so several hits can be opened in a row), and Ctrl+U to clear the query. Unlike the other overlays, neither Return nor Escape discards content search's state: opening a result doesn't close the overlay, and Escape (which does close it) leaves the query, results, mode, selection, and disclosure state exactly as they were, reset only when the user explicitly clears or changes the query. `internal/search`'s matching (both modes)/binary-detection/cancellation logic is unit-tested; the overlay wiring itself (both entry points, live query typing including literal spaces, regex-mode toggling and error display, disclosure toggling, jump-to-line accuracy, Escape-preserves-state, the slow-scan spinner, and the index badge rendering in this overlay too) is hand-verified in a real terminal alongside the rest of `internal/ui`.

**Post-stage fixes**: the browser and quick open toggle keys were corrected from `B`/`O` to lowercase `b`/`o` (a typo relative to `specs/SPEC.md`'s intent of single-character, non-shifted keybindings); the browser's Space and `a` open actions were collapsed into a single open action that always opens a file into the primary preview without closing the browser (`a`'s "open, keep open" behavior is now the only behavior, since closing on open turned out to not be worth keeping), and that action's key was subsequently moved from Space to Return; the top menu bar now always shows the tree root path (abbreviated with `~` for the home directory), left-aligned, rather than the full path of whatever file happens to be open, with the keybinding legend right-aligned — dropping the root path first if the terminal is too narrow to fit both — and the open file's own root-relative path is shown in a separate title bar directly above the preview content; every overlay's header now left-aligns its own content (query, label, or root path) against a right-aligned keybinding legend the same way; Escape no longer quits from the primary preview view (only `q` does), to avoid an accidental Escape discarding the session's open-files state; every keybinding legend/header now labels the confirm key `Return` rather than `Enter`, matching the physical key on a Mac keyboard; and quick open's `o`/Escape "toggle" was found to break ordinary typing (any filter query containing the letter "o" — a common one — closed the overlay instead of filtering), so quick open no longer treats `o` as a close key once open, leaving Escape as the only way to close it (the browser's `b` toggle is unaffected, since the browser isn't a text-entry surface); and quick open is now also reachable with `o` from within the browser overlay (previously primary-preview-only), replacing the browser view the same way jump to file and content search already do — Escape returns to the browser if that's where it was opened from, while successfully opening a match always lands on the primary preview view regardless of entry point, mirroring content search's existing entry-point handling; and file-specific actions (currently just goto-line, `g`) moved out of the global header/title bar's legend and into the file title bar's own right-aligned legend, shown only where the preview is actually interactive (omitted, path-only, in the split-view browser's read-only preview pane) — the global bar is now app-wide-actions-only, and new file-specific actions belong in the file title bar going forward.

**In-file find (spec §2.4)**, added after the fixes above: a `less`-style `/`-triggered search within the currently-displayed file's own content, distinct from quick open/jump to file (which match paths, never content) and content search (which matches content across the whole tree, not one open file). The prompt and live status (query, current/total match count, and a "wrapped to top/bottom" note) live in the file title bar rather than a separate row, per the file-specific-actions convention above; every match is highlighted in the preview itself (the current match distinguished from the rest), and `n`/`N` step between matches with wraparound. Find state is tracked per open-files entry, independent across files, the same way scroll/goto-line already are. Escape, initially inert here as everywhere else at the primary view, was given one narrow exception: it clears an active find (otherwise there was no way to dismiss the highlighting/status short of running a new search) — it's still not a general "back out" key and still never quits.

**Rendering fixes**, found while investigating "mangled" previews: (1) `internal/preview.Highlight`'s token-to-line attribution had a bug where a chroma token whose value spanned a newline (a whitespace token covering "end this line, then indent the next one," e.g. `"\n    "`) attributed the text *after* the newline to the line that had just ended, since the line index was only advanced after the whole token was processed rather than at the newline itself — this silently dropped a line's leading indentation and shifted everything after it, most visibly on YAML files (heavily indentation-dependent, and chroma's YAML lexer produces exactly this token shape constantly); fixed by advancing the line index as each newline is encountered mid-token, interleaved with flushing the surrounding text to the correct (old-or-new) line. (2) Raw tab characters were never expanded — they reached the terminal as a single narrow column instead of the width a terminal normally gives them — visibly breaking alignment on any tab-indented file (Go source among them, being gofmt'd); fixed by expanding tabs to 8-column tab stops once at read time, before highlighting/wrapping/guttering/find ever see the text, so none of them need their own tab-width handling. Both are unit-tested: a regression test reconstructs every highlighted line's text from its segments and compares it to the source line, and a tab-expansion test covers both a mid-line tab and tab stops resetting per line. `internal/find`'s matching logic is unit-tested; the overlay-free wiring itself (prompt open/typing/cancel/execute, `n`/`N` stepping and wrap notes, scroll-to-match, and highlighting including in the split-view browser's read-only preview pane) is hand-verified in a real terminal alongside the rest of `internal/ui`.

**Copy mode (spec §2.1)**, added after a report that mouse-selecting preview text also grabbed the line-number gutter: a `c`-toggled display preference that strips the gutter and syntax-highlighting color from the preview, leaving a screen with nothing on it but the file's own characters at full pane width — color was never actually a selection risk (tcell applies it via terminal escape sequences outside the text stream, not literal codes a selection could copy), but the gutter genuinely was. In-file find's match highlighting stays visible in copy mode regardless, since it's not selectable text either and remains useful while locating what to copy. It's tracked per open-files entry rather than globally (initially built global, then moved per-entry once it became clear a copy-mode preference for one file has nothing to do with the next file you switch to — the same reasoning behind scroll/goto-line/find already being per-entry), so it applies wherever that entry's preview is drawn, including the split-view browser layout's read-only pane; the file title bar renders in a visually distinct style and tags its content `[copy mode]` whenever the displayed entry is in copy mode, regardless of which of the row's other states (idle, active find, find prompt) is otherwise showing, so it's always legible rather than only appearing in the idle case. Hand-verified in a real terminal alongside the rest of `internal/ui`.

**Word-wrapping**, replacing the original fixed-column wrap: `wrapLineSegments` now only breaks mid-word as a last resort (a word that doesn't fit within a full-width row even on its own) — otherwise it breaks at the end of a run of spaces (dropping them, the standard word-wrap convention) or immediately after a dash/hyphen (keeping it, since the dash is itself the visual break marker). Unit-tested for the space-preferred case, moving a word whole to the next row rather than splitting it, dash breaks, the mid-word hard-break fallback, and triggering-space-dropped-vs-real-trailing-space-preserved.

Word-wrapping briefly led copy mode astray: reasoning that a multi-row mouse selection across wrapped continuation rows copies a line break the file doesn't actually have, copy mode was changed to disable wrapping entirely and clip a long line at the pane's edge instead. That traded a cosmetic problem for a functional one — anything past the clipped edge was never drawn at all, so it couldn't be selected by any means, making it impossible to copy an entire long line rather than just occasionally picking up an extra line break in a multi-row selection. Reverted: copy mode wraps exactly like normal display (gutter/color are still stripped), so every character of every line stays visible and selectable somewhere on screen, accepting the wrap-induced-line-break limitation as inherent to any fixed-width terminal grid rather than something worth losing content over.

**Jump to file reworked from a fuzzy finder into an in-browser incremental jump (spec §4.3)**: it no longer opens a full-screen flat-list overlay searching the entire background-indexed tree — that behavior now belongs solely to quick open. Jump to file is instead a typing mode layered directly on the still-visible browser: candidates are exactly the browser's current flattened row list (whatever the tree's live expand/collapse state already exposes, so it never itself expands or collapses anything on the user's behalf), matched by case-insensitive prefix against each row's own leaf name rather than its full path. Typing live-moves the browser's own selection to the first match; Tab/Shift-Tab (or Down/Up) cycle among several matches in display order; Return simply leaves jump mode with the selection wherever it landed; Escape restores the exact selection/scroll from before `/` was pressed. Because jump to file no longer expands ancestor directories to reveal a match, `tree.RevealPath` (and its "resolution failure" case) became dead code and was removed along with its tests. The new candidate/matching logic is pure and unit-tested (`match.PrefixMatches`, `tree.JumpMatches`); the mode's key handling and live-highlighting are wired in `internal/ui` and hand-verified in a real terminal alongside the rest of that layer.

**Open-files list overlay redesign (spec §2.3)**: the overlay no longer fully replaces the screen — it now renders as a small dropdown-style popup over the (unmodified) primary preview view, the same "floating window over an unchanged background" idiom the browser's popup layout already used, just without that layout's split-view alternative (the open-files overlay is always a popup). It shows at most 10 entries at a time ("a page"), each row labeled with its 0-9 position on that page so a digit key jumps straight to and opens that entry, closing the overlay immediately — a faster path that coexists with the existing arrow-keys-then-Return flow rather than replacing it. Page Up/Page Down turn a full page at a time (clamped at the first/last page, not wrapping, mirroring the file preview's own Page Up/Down semantics); Shift-Page-Up/Shift-Page-Down do the equivalent bulk move for reordering, since walking an entry across several pages one Shift-Up at a time was impractical. Which page is showing, and each row's on-page digit, are derived from the selected index every frame (`internal/openfiles.Page`/`PageBounds`) rather than stored and kept in sync — so removals and reorders re-flow the visible numbering for free. The popup's border carries a "▲"/"▼" at the top/bottom when an adjacent page exists, and a counter ("11–20 of 23") plus keybinding legend renders inside the box. The pure paging/bulk-reorder math (`Page`, `PageCount`, `PageBounds`, `SelectPage`, `SelectDigit`, `List.MoveUpPage`/`MoveDownPage`) is unit-tested in `internal/openfiles`; the dropdown's rendering itself is hand-verified in a real terminal alongside the rest of `internal/ui`.

**Browse, quick open, and content search made mutually exclusive (spec §5.1)**: `o` (quick open) and `s` (content search) had each been reachable both from the primary preview view and from within the browser overlay (and, for content search, from within either), replacing the browser view in place while remembering it as the "return to" screen on Escape. That made it possible to chain from one file-selection mode directly into another without ever passing back through the primary preview view, which made it easy to lose track of which mode's in-progress state (a browser selection, a quick open query, a search) was about to be abandoned. All three are now reachable only from the primary preview view: `o` and `s` are no-ops from within the browser overlay, `s` is a no-op from within quick open, and `o` is a no-op from within content search — Escape from quick open or content search always lands back on the primary preview view rather than on whichever overlay it happened to be entered from. This removed the `quickOpenEntryPoint`/`searchEntryPoint` tracking entirely, along with the browser's now-dead `o`/`s` key handling; the browser's own header legend was updated to stop advertising them. The open-files list overlay (`Tab`) is unaffected — it isn't one of the three file-selection modes and remains reachable only from the primary preview view as before.

**Header/title bar mode labels**: the header/title bar's left side now shows a bold, all-caps mode label (`BROWSE`, `QUICK OPEN`, `SEARCH`/`SEARCH (REGEX)`, `JUMP`) in place of the tree root path whenever a mode other than the primary preview view is active, similar to how modal editors like `hx` always surface the current mode — only the label itself is bold, the legend sharing the row stays normal weight. Jump to file's query, which previously appeared directly in the header (`/query`), moved to its own input row directly below the header instead, the same convention quick open and content search already use for their own queries — user-entered text no longer appears in a row now reserved for the mode label.

**Quick open and content search reveal the opened path in the browser (spec §4.2, §9.2)**: opening a file from either overlay now expands every intermediate ancestor directory and moves the browser's selection to it, so pressing `b` afterward shows where that file lives without further navigation, rather than leaving the browser wherever it last was. This resurrects `tree.RevealPath`, removed earlier when jump to file was reworked into an in-browser incremental jump that deliberately never expands anything on the user's behalf (see above) — that reasoning doesn't apply to quick open/content search, since both search the whole tree regardless of current disclosure state and have no other way to reflect what they just opened.

**Browser overlay's split-view/popup dual layout removed (spec §5.1, issue #35)**: a bug surfaced where jump-to-file's query input row was drawn at full terminal width instead of the browser pane's own width, bleeding into the read-only preview pane in split view. Investigating it raised a bigger question: the split-view preview pane was never a live preview of the browser's selection — it just kept showing whichever file had last been opened, static, until Return opened something new — so weighed against that narrow benefit, the dual layout's ongoing cost (a whole extra layout algorithm, two separate draw paths sharing screen real estate differently, and a recurring class of pane-boundary bugs like this one) wasn't worth it. The browser overlay now always renders full-screen at every terminal width, the same shape as quick open: a header row plus the flat row list filling the rest of the screen. This removed `internal/layout` entirely, `computeSplitLayout`/`browserPaneWidth` from `internal/ui/app.go`, and `drawBrowserSplitView`/`drawBrowserPopup` from `internal/ui/render.go` (folded into a single `drawBrowserOverlay`). Hand-verified in a real terminal: the browser and jump-to-file render full-width at both narrow and wide terminal sizes, the jump query row no longer bleeds past its own content, and live resize no longer flips between two layouts (there's only one now).

**Toast primitive extracted, then a unified red error flash added for any failed operation on a row (spec §5.3, §2.2, §6.1)**: the bottom-right badge's completion-message fade math moved out of `internal/spinner` into a new `internal/toast` package (`Decide`), a standalone "show in full, then fade left-to-right from an anchor point" primitive; `spinner.BadgeDecision` now calls it directly, with no behavior change. A live-refresh discovering that an already-loaded directory newly lost read permission was first surfaced as a toast built on that same primitive, then reconsidered: an error is diagnostic text the user may be reading, and a toast that fades out on its own schedule risks disappearing mid-read. Redesigned into a brief red flash on the affected row instead (`styleFlashError`, the same brief-attention mechanism as the existing green post-open flash) with the failure message appended inline on that row — the flash decays, the inline text does not, staying until overwritten by a later success. That treatment then generalized to cover every open-failure in the app, not just directory-listing errors: a file that fails to open (permission denied, binary) from the browser, quick open, or content search now gets the exact same red flash + inline message on its own row, replacing the previous separate bottom-status-line messages (`browserMessage`/`finderMessage`/`searchMessage`, now removed) those three used before. The browser reuses `tree.Node.Err` directly (the same field a directory-listing failure already sets); quick open and content search track the failed path/message separately (`finderErrorPath`/`searchErrorPath` et al.) since their rows are rebuilt from the index/search results every keystroke rather than being stable node identities — cleared whenever the query changes. A clipboard-copy confirmation toast was considered (`docs/ANIMATION_IDEAS.md`) but deliberately not built: there's no clipboard-copy action in dirtree to confirm yet. `internal/toast` and `tree.RefreshTree`'s newly-erroring reporting are unit-tested; the flash/inline-message rendering across all three overlays is hand-verified in a real terminal alongside the rest of `internal/ui`.

Three refinements followed: (1) a directory failing to load when the user directly expands it (Right-arrow) now gets the same red flash the live-refresh case already had — previously only a live-refresh-detected failure flashed; an expand-triggered one showed the inline error but no flash. (2) Every open-failure message (`tree.errText`/`preview.errText`) now strips the path Go's `*fs.PathError` normally repeats (e.g. "open /a/b: permission denied" becomes just "permission denied") — redundant once the message is already shown inline on that path's own row. (3) `tree.Node.LoadChildren` previously marked a directory "loaded" even when the read failed, so a failed load's error lingered forever — collapsing and re-expanding it (or pressing Right again) silently no-opped instead of retrying, even after the underlying problem (e.g. permissions) was fixed. It now only skips a retry once a load has *succeeded*, so fixing the problem and re-expanding actually clears the error rather than requiring a live-refresh to happen to notice on its own.

**Live reload of open files (spec §6.1a)**: an open file's preview content used to only ever reflect whatever was on disk at the moment it was opened — editing it in another program while dirtree had it open left the preview silently stale until the user closed and reopened it. Since every open file necessarily lives in a directory that's already loaded, and therefore already watched (§6.1), no new watch plumbing was needed: `openfiles.List.Reload` now runs on every debounced live-refresh, comparing each open entry's on-disk mtime against the mtime recorded at its last (re)load, re-reading and re-highlighting only the ones that actually changed — for every open entry, not just the currently-displayed one, so a background file is never stale by the time the user switches to it. A reload mutates the existing entry in place (same identity, same list position, same displayed status) and invalidates its line-wrap cache and any in-file find state, since both are derived from content that just changed; scroll position is left alone rather than snapped back to the top, so it self-clamps naturally the next time the entry is scrolled or displayed. A file that's deleted or becomes unreadable between checks is left with its last-known content rather than being reloaded or cleared, matching §6.1's existing deleted-file handling. This is the first real caller of the generic toast primitive introduced above (previously only used internally by the indexing badge): a successful reload surfaces a bottom-right toast naming the reloaded file(s), reusing `drawCornerBadge`'s anchor/fade — unlike the directory-error case above, this is routine confirmation rather than diagnostic text, so letting it fade on its own schedule is the right call here. `openfiles.List.Reload` is unit-tested (changed-vs-unchanged mtime, multiple open files, entry-identity/list-order/displayed-index preservation, the deleted/unreadable case, and wrap-cache/find-state invalidation); the toast rendering and end-to-end live-reload behavior are hand-verified in a real terminal alongside the rest of `internal/ui`.

**Minimum terminal size (spec §6.4)**: below 58 columns or 15 rows, none of the app's layouts (header + legend, popups, split content) can render without truncating or overlapping in misleading ways, so `draw` now checks the terminal's current dimensions first, every frame, and — if either is below the minimum — replaces the entire frame with a dedicated too-small screen instead of whatever view/overlay was active. Rather than a block of text, it draws the single undersized dimension as red arrows pointing apart with the required size centered between them (`← 58 →` on the vertical-center row for width, a vertical `↑`/`15`/`↓` stack for height), addressing individual cells directly rather than through any row-width helper so it stays legible even far below its own minimum; when both dimensions are short, only one indicator is shown at a time (whichever is proportionally further below its own minimum, ties favoring width) rather than trying to fit both into a space too small for either. Key input is suppressed for the duration (checked in the main loop before dispatching to `handleKey`), so nothing typed while it's showing can change app state; resizing back above both thresholds simply resumes drawing whatever was already there, untouched. Hand-verified in a real terminal: resizing narrower than 58 columns, shorter than 15 rows, and both at once (confirming the proportionally-worse dimension's indicator is the one shown, including a tied case favoring width) from each of the primary preview view and every overlay; resizing back restores the exact prior view; and the too-small screen itself doesn't crash when resized far below its own minimum (down to a handful of columns/rows).

**Legend consistency, ordering, and narrow-terminal hierarchy (spec §5.2, §7)**: every keybinding legend in the app now follows one convention — primary action, then secondary/navigation actions, then destructive/clear actions, then escape/cancel/close last — and each entry carries a drop priority for narrow terminals, replacing the old binary "drop the left-hand label, then hard-clip the legend text" behavior. `internal/ui.legendFit` tries the full legend alongside the left-hand content (root path, mode label, file path) first, then the full legend alone, then drops whole priority-3 entries, then whole priority-2 entries — never reordering survivors or truncating an entry mid-text. Several real, already-working keybindings that had no legend entry anywhere were filled in: jump to file's Shift-Tab/Up previous-match and `/` slash-to-expand, quick open's and content search's Tab/Shift-Tab row navigation, the open-files list's Enter-to-open, and the goto-line prompt (which previously had no legend at all). `legendFit`'s tiered fit/drop logic is unit-tested in `internal/ui`; the visual result of resizing through each tier's drop points is hand-verified in a real terminal alongside the rest of that layer.

**`b` is no longer a toggle on the browser overlay (spec §5.1, §7)**: pressing `b` while the browser is already open used to close it, mirroring quick open's short-lived `o`-as-close-key behavior (removed above for the same reason). Escape is now the sole key that closes any overlay, browser included — `b` inside the browser overlay is simply a no-op, matching the rest of the app's overlays. This mainly removes a footgun for jump-to-file queries starting with `b`, since jump mode already intercepted `b` before it could reach the toggle, and keeps "how do I close this" a single consistent answer across every overlay.

**Content search's regex-mode indicator moved off the header (spec §9.2)**: the header's mode label used to switch between `SEARCH` and `SEARCH (REGEX)` depending on Ctrl+R's toggle state, which competed with the keybinding legend for the header row's width and disappeared entirely once a narrow terminal dropped the mode label. The indicator now lives on the query-input row instead, prefixing the prompt (`[regex] > needle` vs. plain `> needle`) — that row never competes with the legend for space and is never dropped, so the indicator stays visible at any terminal width. The header's mode label is now always the plain `SEARCH`, matching `BROWSE`/`QUICK OPEN`/`JUMP`'s single-state labels.

**Startup no longer auto-opens the browser (spec §1, §5.1)**: the app now starts on the primary preview view's empty state with no overlay active, superseding stage 2's original "browser auto-opens on startup" behavior described above — pressing `b` opens the browser same as always. The open-files list overlay auto-opening the browser when its last entry is removed (§2.3) is unchanged, since that's a separate, still-live behavior. Along with this, several other small UX fixes landed together: the browser's own legend now advertises left/right expand/collapse (previously a real, working keybinding with no legend entry); content search's hit rows are now indented further than their owning file row's own path text, so they read as visually nested rather than lining up with it; Tab/Shift-Tab were dropped from quick open and content search (both already had Up/Down for the same navigation, making the second binding low-value) — jump to file's own Tab/Shift-Tab is unaffected; and jump to file's header/title bar mode label no longer switches to a distinct `JUMP` label while active, staying `BROWSE` throughout (only the legend and query row change), its query-row prompt changed from `/query` to `> query` to match quick open/content search's own prompt convention, and slash-to-expand no longer clears the query row back to blank with no trace of what was typed — each committed path segment now accumulates in a disclosed-path prefix shown ahead of the live query (e.g. `> internal/ui/`) instead.

**Content search streams file scanning instead of a byte-capped read (spec §9.1, §9.2, issue #64)**: each candidate is now read line by line with no size cap, rather than reading only up to the same 1MB cap preview loading uses — a match anywhere in a file is found regardless of file size, not just within the first megabyte. Since there's no cap to bound scan time, each candidate's scan is instead bounded by a per-file timeout, and candidates are scanned with bounded concurrency (a fixed number at once) rather than one at a time, so one slow/huge file's timeout doesn't serialize in front of the rest of the candidate set. A file that hits the timeout, or fails to open or read partway through (permission denied, removed mid-scan), is no longer silently dropped: it's still listed, with whatever matching lines were found before the problem, and the problem itself shown inline on that file's row in the same contrasting error style the invalid-regex message already uses — distinct from, and independent of, the existing transient red open-failure flash, which is about a failed *open* action rather than a failed *scan*. Binary detection is unchanged in spirit (still a silent, intentional non-match, not a reported issue), just sniffed from a candidate's leading bytes rather than derived from the whole capped read. `preview.errText` was exported as `preview.ErrText` so content search could reuse the same OS-error-message formatting preview's own failed-open handling already used. `internal/search`'s streaming/timeout/concurrency/issue-reporting behavior is unit-tested, including a regression test for a fan-in deadlock found during development (dispatching more candidates than the concurrency limit while nothing yet drained results could wedge the scan permanently — fixed by running dispatch and result collection concurrently instead of sequentially); the inline issue rendering is hand-verified in a real terminal alongside the rest of `internal/ui`.

**Directory expand/collapse glyph unified, browse/search navigation extended (spec §3.4, §5.2, §9.2)**: the browser's expand/collapse marker used plain ASCII (`v `/`> `) while content search's own file rows used Unicode triangles (`▾`/`▸`) for the identical concept — the browser now uses the same `▾`/`▸` pair, so the indicator reads as one consistent glyph across every view that shows it. The open-files-list legend's `[shift+↑↓] move` entry similarly stood alone in embedding arrow glyphs in its label text where every other legend in the app spells the keys out in words — changed to `[shift+up/down] move` to match. Separately, three navigation gaps were filled in: content search gained Tab/Shift-Tab to jump the selection directly to the next/previous file's own row (skipping over hit rows in between, wrapping at both ends) alongside Page Up/Page Down to page the row list by a full visible-height jump — both join the existing Up/Down one-row and Left/Right disclosure keys, and both are reflected in the legend; the browser gained the equivalent Page Up/Page Down paging (§3.4); and the browser's Return, previously a no-op on a directory, now discloses it (toggles expand/collapse, loading children if needed) the same way `/`-then-Return or Right/Left would, so Return is no longer file-only. Hand-verified in a real terminal: the browser's marker glyphs match content search's at both expanded and collapsed states; Tab/Shift-Tab and Page Up/Page Down move the selection correctly in both the browser and content search, including wraparound/clamping at the ends of the list; and Return on a directory in the browser expands it from collapsed, collapses it from expanded, and still opens a file normally when the selection is a file.

**Indexing badge widened to render in every view (spec §5.2)**: the bottom-right delayed-loading badge previously only rendered in the browser, quick open, and content search overlays (`draw()` called `drawBadge` from inside three of the switch's five cases) — the primary preview view and the open-files-list overlay never showed it, so on a large enough tree the spinner could go unnoticed unless the user happened to already be in one of those three overlays. `drawBadge` is now called once, unconditionally, after the view-dispatch switch, so it renders identically regardless of which view is on screen. Hand-verified in a real terminal with `-tags spinnerdebug`: the badge's spinner/completion-message/fade sequence appears the same way on the primary preview view, the open-files popup, and all three overlays.

**Background line-offset index and goto-line gating, stage 1 of the streaming-preview design (spec §2.1, §5.2, §5.3, `docs/STREAMING_PREVIEW_DESIGN.md`)**: every opened (or reloaded) entry now also starts a background pass (`preview.StreamIndex`, `internal/preview/stream.go`) that streams the file once more and records the byte offset of every line's start, independent of and non-blocking to the existing capped read/highlight/wrap pipeline, which is otherwise unchanged — this is groundwork for later stages' windowed reads and Tier A/B highlighting split, not yet consumed by them. While an entry's pass is still running, pressing `g` no longer opens the goto-line prompt: the file title bar instead briefly flashes red and shows an inline "still indexing, try again shortly" message, the same red-flash-plus-inline-message convention already used for a failed file open, and the file title bar's idle legend replaces `[g] goto line` with a "building…" spinner once the pass has been running long enough to be perceptible, reusing the corner badge's own threshold/minimum-display-duration timing rather than inventing new rules for a second indicator. `StreamIndex`'s offset-building (including empty-file, no-trailing-newline, and unreadable-file edge cases), the goto-line block/allow decision, and the file-legend spinner's show/hide decision are all unit-tested as pure functions; hand-verified in a real terminal against a ~500MB generated file: the "building…" spinner shows in the idle legend while the pass is running, pressing `g` during that window flashes the title bar red with the inline message instead of opening the prompt, and both revert to normal (`[g] goto line`, working goto-line) once the pass finishes.

**Two-tier highlighting split and windowed reads for large files, stage 2 of the streaming-preview design (spec §2.1, §2.4, §5.2, `docs/STREAMING_PREVIEW_DESIGN.md`)**: `openfiles.List.Open`/`Reload` no longer read or highlight a file synchronously at all — a small capped peek (`preview.ReadCapped`) still detects a failed-open/binary file fast and synchronously, exactly as before, but once that passes, the entry's real content is built entirely by its stage-1 background pass, decided once from the file's size against a new `preview.HighlightCeiling` (default 25MB, a `var` for later tuning like `search.go`'s own knobs): at or under the ceiling (`preview.TierHighlighted`), the pass reads, decodes, and highlights the whole file in the background — the same content the old synchronous path produced, just non-blocking — and it's kept fully resident, same as before. Over the ceiling (`preview.TierPlainText`), the pass builds only the line-offset index; no highlighting is attempted (every line renders plain text) and the file is never held resident — `preview.ReadWindow` seeks directly to a requested line's byte offset and reads just enough lines to fill the viewport plus a scroll margin, so memory use for an arbitrarily large file stays proportional to the viewport rather than the file. The tier is re-decided from the file's current size on every reload rather than fixed at open time — reload already `Stat`s the file to compare mtimes, so re-checking size against the ceiling at that same moment is free, and it's the only point where a stale tier decision (e.g. a file that's grown well past the ceiling since it was opened) could otherwise ever get corrected; a tier flip also resets the entry's scroll position to the top, since a display-row index means something different under each tier's model. A new `openfiles.Entry.SyncContent` pulls a finished `TierHighlighted` pass's content into `Entry.Lines`/`Segs`; the preview view's `contentReady` does the same check for both tiers (gating on `Stream.Done()` for `TierPlainText`, a deliberate simplification of the design's more ambitious "content available before the whole pass finishes" aspiration — flagged in `SPEC.md` rather than silently landing as the ideal) and drives a new "building preview…" content-area placeholder shown until then. `TierPlainText` scrolling and goto-line are, of necessity, a real change to the scroll model (also flagged in `SPEC.md`): since that tier only ever holds a windowed slice rather than a whole-file wrap cache, Up/Down and Page Up/Page Down move by source-line count rather than display-row count; goto-line and the line-number gutter (floored at 4 digits while the pass is still running, per the design's own gutter-flicker mitigation) both work against the same windowed model. In-file find (`/`) is a no-op on a `TierPlainText` entry for now — explicitly out of scope for this stage per the design's own phasing, to follow in a later stage. `StreamIndex`'s tier-aware content building, `ReadWindow`'s seek-based reads (including past-end-of-file truncation), the tier decision itself, reload's tier re-derivation (both directions) and its scroll-reset-on-flip behavior, and the windowed scroll/goto-line/gutter logic are all unit-tested; hand-verified in a real terminal against a ~2.5GB generated file (40M lines): the file opens immediately showing a "building preview…" placeholder while its background pass runs, then renders correctly once done, with Up/Down scrolling by source line, goto-line jumping directly via seek (confirmed against a line 20+ million lines in), and the gutter showing the exact final line count.

**Async in-file find over the plain-text tier, stage 3 of the streaming-preview design (spec §2.1, §2.4, §9, `docs/STREAMING_PREVIEW_DESIGN.md`)**: `/` is no longer a no-op on a `TierPlainText` entry (stage 2's interim limitation, since that tier never holds full content resident for `find.InLines` to search synchronously). A new `internal/find.Scan` (`internal/find/scan.go`) mirrors `preview.StreamIndex`/`index.Index`'s own background-pass shape: `performFind` starts a cancelable, streamed line-by-line scan for that tier instead of searching in memory, leaving the match list empty until it reports done; a new `Preview.syncFindScan`, called once per frame from `Draw`, picks up a finished scan's result, seeds the current match (the same "search forward from the top of the viewport, wrapping and noting the wrap otherwise" logic a synchronous find's result already got, now shared via a new `seedFindCurrent` helper), and scrolls to it — fetching the entry's on-screen window to cover the match's line first if it falls outside what's currently loaded, the same way goto-line already does. The file title bar's status area shows the query alone until the scan crosses the same perceptibility threshold the corner badge and content search's own scan spinner use, then a "searching…" spinner in place of a match count. Escape (or a new query superseding an old one) cancels a still-running scan rather than leaving it to finish unread; `openfiles.List.Reload` does the same for an entry's in-flight scan alongside its other find-state invalidation. `findMatchRow`/`findHighlightsForRow` now convert a match's absolute source line into window-relative coordinates before comparing against `e.Rows`' `SourceLine` (a no-op subtraction for `TierHighlighted`, whose window always starts at line 0) — needed because match line numbers come from a full-file scan regardless of what's currently loaded in a `TierPlainText` window. `find.Scan`'s streamed matching (including empty-query, unreadable-file, and cancellation cases), the async find lifecycle (start/sync/clear/reload-cancels-and-clears), and the window-relative match-row lookup are all unit-tested; hand-verified in a real terminal against a ~1.3GB/20M-line generated file with a unique needle inserted 15 million lines in: `/` opens the prompt on the plain-text-tier file, the query shows without a spinner briefly then with one once the scan is perceptible, the single match is found and highlighted at the correct line once the scan finishes, and Escape clears the highlight and status back to the idle legend.

**Memory-footprint audit fix: per-node/per-entry redundant path strings removed**: a memory audit found two spots holding fully-derivable data as separate resident strings on every tree node or index entry, for the app's entire lifetime. `tree.Node` no longer stores `Path`/`Depth` fields — both are fully reconstructable by walking `Parent` (the root records its own absolute path once, in a new unexported `rootPath` field; every other node's path is rebuilt by joining `Name` segments up to the root), exposed as `Node.Path()`/`Node.Depth()` methods instead of fields, called from the same render-loop and event-handling call sites that used to read the fields directly. `index.Entry` similarly no longer stores `RelPath` alongside `AbsPath` — quick open, content search, and the index's own build-time sort now derive it via a new `Entry.RelPath(root string) string` method. On a 100k-node/100k-file tree this removes on the order of 30MB of duplicate string data; per SPEC.md's terminal-rendering-free design discipline, both changes are confined to the pure `internal/tree`/`internal/index` layers plus their call sites, with no behavior change (verified by the existing test suite, which required no logic changes beyond updating field-access call sites to method calls).

**Resident-content LRU eviction for the highlighted tier (spec §2.1, `internal/openfiles/openfiles.go`)**: a memory-footprint audit found that stage 2's `TierHighlighted` entries were kept fully resident (`Entry.Lines`/`Segs`) for the entry's *entire session lifetime* rather than just while actually being looked at — the open-files list has no cap on how many files can be open at once, and nothing ever cleared an entry's content short of the user manually closing it with `x`, so a handful of files opened near `HighlightCeiling` (25MB) and left open could retain 250MB+ resident even though only the currently-displayed entry is ever on screen. `openfiles.List` now tracks the `ResidentCap` (4) most-recently-*displayed* entries in MRU order (`List.resident`, updated by a new `touchResident` called from `Open`, `Display`, and `Open`'s reuse-by-path branch); displaying an entry beyond that count evicts the least-recently-displayed one's `Lines`/`Segs` via a new `Entry.evictContent`, which also restarts that entry's background `Stream` (the same `preview.StartStream` call `Open`/`Reload` already use) so `SyncContent` transparently rebuilds the content — re-read plus re-highlight — the next time the entry is displayed, rather than leaving it permanently empty. `Remove` drops a removed entry out of residency tracking (`forgetResident`) so it isn't kept reachable, and thus uncollectable, by a stale pointer in the resident list after it's left the open-files list entirely. This is a real memory/latency tradeoff, not a free win: switching back to an entry that fell out of the resident set now briefly shows the same "building preview…" placeholder a freshly-opened file shows, for the same duration its original open took, rather than the previously-instant display it got before this change — mitigated by keeping the cap small enough (4, matching the "simple fixed constant, tuned later against real usage" stance `internal/ui/views/scroll.go`'s own `windowLines`/`windowMargin` already take) that ordinary quick tab-switching among a few recently-viewed files still finds their content resident and pays no extra cost. Deliberately scoped to eviction alone: the entry-level duplication between `Lines` (raw text) and `Segs` (the same text re-split into per-token segments, each copying its own `Text string` rather than referencing offsets into the line) is a separate, larger change and was left as a follow-up rather than folded into this one. `List.touchResident`/`forgetResident`/`Entry.evictContent` are unit-tested (eviction beyond the cap, transparent repopulation on redisplay, other entry state left untouched by eviction, the currently-displayed entry never being evicted, `TierPlainText` entries never being evicted, and `Remove` not leaving a stale resident-tracking reference behind); not separately hand-verified beyond the existing preview rendering coverage, since eviction only changes *when* `Lines`/`Segs` are populated, not what ends up rendered.

**File title bar shows the displayed file's line count (spec §5.2)**: the file title bar's left-hand content, previously just the displayed entry's root-relative path, now leads with its total line count (`1 line`/`N lines`) ahead of that path — e.g. `120 lines  two.txt` — reusing `bestLineCount` (`internal/ui/views/scroll.go`), the same tier-aware helper `gutterWidth` already uses: `len(e.Lines)` for a `TierHighlighted` entry, or `e.Stream.Snapshot()`'s line count for `TierPlainText`, which reads as a live-growing count while that entry's background pass is still running and settles once it's done. The count and path are prepended as one string before any of the row's other left-side content (the `[copy mode]` tag, an active find's status, or the find prompt) is layered on top, so it participates in the same fit/drop behavior as the rest of the row's left side rather than being dropped independently. Covered by `TestDrawFileTitleBarShowsLineCount` and `TestDrawFileTitleBarShowsSingularLineCount` (`internal/ui/views/preview_test.go`); not separately hand-verified beyond that, since this only changes the row's text content, not its layout/fit logic (already covered by the existing legend-fit tests) or `bestLineCount` itself (already exercised via the existing gutter-width tests).

**Quick open gains the open-file indicator (spec §4.2, §2.2, §9.2)**: quick open's flat match list previously had no way to tell, at a glance, which of its results were already open — the browser and content search both already show a lasting "●" marker on a row already present in the open-files list, but quick open's own rows (`drawList`, `internal/ui/views/quickopen.go`) never did. Each row now checks `v.Files.IsOpen(m.AbsPath)` (the same `openfiles.List` method the browser and content search already call) and prefixes its path with "●" plus a space when open, or a blank placeholder space in the same width otherwise, so the path's own starting column never shifts depending on which rows happen to be open. Covered by `TestQuickOpenDrawListShowsOpenIndicator` (`internal/ui/views/quickopen_test.go`), which draws two matches — one already open, one not — and asserts each row's leading marker; not separately hand-verified beyond that, since this only changes the row's text content, not quick open's existing selection/scroll/paging behavior.

**Closing the last open file no longer auto-opens the browser (spec §2.3, §5.1)**: removing the open-files list's final entry (`x` from the open-files overlay) used to set `Overlay` to `OverlayBrowser`, on the theory that emptying the list mid-session warranted an immediate way to open something else, unlike a fresh startup (which lands on the plain empty preview state per §1). In practice this made the two "no files open" states behave inconsistently for no real benefit — the empty preview state's own hint already tells the user how to open a file — so the open-files overlay's `x` handler (`internal/ui/views/openfiles.go`) now sets `OverlayNone` instead, landing on the same empty preview state startup does, with no overlay auto-opened on top. `specs/SPEC.md` §2.3 and §5.1 updated to describe this uniformly instead of carving out a "distinct case from startup." Covered by `TestOpenFilesRemoveLastEntryReturnsToEmptyPreview` (new) and `TestOpenFilesRemoveNonLastEntryStaysOnOverlay` (a regression guard for the unaffected not-emptied path), both in `internal/ui/views/openfiles_test.go`.

**Content search shows a "N hits across N files" summary (spec §9.2)**: the query row previously showed only the typed query itself; once results exist for the current query, its right side now also shows a right-aligned `N hits across N files` summary (e.g. `3 hits across 2 files`), via a new `searchSummary` helper (`internal/ui/views/search.go`) and `canvas.FitPair` (the existing left-text-plus-right-aligned-content primitive `LegendText`/`LegendFit` already build on) rather than the header row, since the header's `SEARCH` label and its keybinding legend already fill that row. Only files that actually contributed at least one hit count toward either total — a file present in the results solely because of a scan issue (§9.1) contributes to neither — and each noun is singular exactly at 1 (`1 hit`/`1 file`), plural otherwise; if the query text plus the summary can't both fit, the summary is dropped rather than truncating the query. Covered by `TestSearchSummaryCountsHitsAndFilesWithHits` (the counting/pluralization rule, table-driven) and `TestSearchDrawShowsHitSummaryRightAligned` (the actual rendered row), both in `internal/ui/views/search_test.go`.

**Primary preview view's header legend reordered, and its open-files-list entry renamed (spec §5.2, `internal/ui/render.go`)**: `previewLegend`'s left-to-right order changed from browse, open-files list, quick open, search, quit to open-files list, quick open, browse, search, quit — open-files list and quick open (both ways to jump straight to a specific already-relevant file) now lead, ahead of browse (§3) and search (§9), each entry's priority left unchanged so narrow-terminal drop behavior (§5.2) is unaffected, only which survivors render where. Separately, `[tab] open files` was renamed `[tab] switch files`: as a bare noun phrase it read ambiguously next to the legend's other action-verb entries (`browse`, `quick open`, `search`) — unclear whether it meant "view the list of open files" (correct) or "open some files" (not what Tab does). An intermediate `open-files list` fix removed the ambiguity but wasn't verb-centered like its neighbors; `switch files` is both a verb phrase and more succinct, and matches how `specs/SPEC.md` §2 itself already describes this overlay ("the overlay used to switch between them"). Covered by a new `TestPreviewLegendOrder` (`internal/ui/render_test.go`), which pins the exact five-entry left-to-right order and text.

**Header legend's "switch files" entry dims when the open-files list is empty (spec §5.2)**: with no file open, Tab has nothing to switch to or from, so this now renders visually inert rather than looking like every other equally-actionable entry — it still stays in the legend (rather than disappearing) so the keybinding hint doesn't flicker in and out as files are opened/closed. No mechanism existed for styling one legend entry differently from the rest of its row — `canvas.LegendFit`/`LegendText` compose the whole row into one string — so a new `canvas.StyleHeaderDim` (`StyleHeader.Dim(true)`) and `canvas.DrawHeaderDimmed` were added: it draws the row once via `LegendText` at the base `StyleHeader` exactly like `DrawHeader`, then re-draws just the matched entry's own columns (located by a rune-safe substring search over the already-fitted text, a no-op if the entry didn't survive narrow-terminal priority dropping) in the dim style — generalizing `DrawHeaderMode`'s existing "compose once, overlay a styled sub-span" pattern from a fixed column-0 span to an arbitrary legend entry whose position shifts with the row's own right-alignment. `internal/ui/render.go`'s two `DrawHeader(w, a.menuBarText(...))` call sites were consolidated into one `drawPreviewHeader(w)` that checks `len(a.shared.Files.Entries) == 0` and dispatches to `DrawHeaderDimmed` or the plain `DrawHeader` accordingly. Covered by `TestDrawHeaderDimmedStylesOnlyTheMatchedEntry` and `TestDrawHeaderDimmedNoopsWhenDimTextIsDropped` (the canvas primitive itself, `internal/ui/canvas/canvas_test.go`) and `TestDrawPreviewHeaderDimsSwitchFilesWhenEmpty` (the empty-vs-non-empty gating, `internal/ui/render_test.go`), all using a `tcell.SimulationScreen` to inspect actually-drawn cell styles rather than just the composed legend text.

**Empty-state hint reordered to match the header legend (spec §2.1)**: the primary preview view's "no files open" message listed browse, quick-open, then search — the header legend above it lists quick open, browse, search (its own reordering, PR #93) — so the two rows disagreed on which action to reach for first. The hint's wording now matches: "no files open — press o to quick open, b to browse, s to search contents" (also dropping the stray hyphen in "quick-open" to match the legend's own "quick open" spelling). Covered by a new `TestDrawContentEmptyStateHintMatchesLegendOrder` (`internal/ui/views/preview_test.go`), which asserts the three hints appear in that left-to-right order.

**Quick open shows a "N of N files" summary on its query row (spec §4.2)**: once the background index has finished building, the query row's right side now shows how many of the index's files (directories excluded) currently match the query out of its total file count, via a new `quickOpenSummary` helper (`internal/ui/views/quickopen.go`) and `canvas.FitPair` — the same query-row-summary composition content search's own "N hits across N files" (§9.2) already uses. The summary is absent while the index is still building (`v.Matches` is nil then, the same signal `drawList`'s own "indexing…" placeholder already gates on), since neither number is meaningful yet. Covered by `TestQuickOpenSummaryCountsFilesOnly` (the counting rule, directories excluded from the total regardless of query) and `TestQuickOpenDrawShowsFileSummary` (the actual rendered row), both in `internal/ui/views/quickopen_test.go`.

**Ghost-text autocomplete and unambiguous-match highlighting in quick open and jump to file (spec §4.2, §4.3)**: when the current query narrows quick open's matches or jump to file's matches down to exactly one row, that row now renders in a secondary highlight (`canvas.StyleFindCurrent`, reused from in-file find's current-match style — new doc comment on it documents this dual role) distinct from plain cursor-position `StyleSelected`, and, where the query is a literal case-insensitive prefix of the match, the remainder renders as dimmed "ghost text" (new `canvas.StyleQueryGhost`, `StyleSearchInput.Dim(true)`) immediately after the typed query on the query row. A new `match.Remainder(query, candidate) (string, bool)` centralizes the "literal continuation of what was typed" primitive both views share. The two views differ in how often ghost text actually appears: jump to file's matching is already a strict case-insensitive prefix match on the leaf name (SPEC.md §4.3), so `Remainder` always succeeds for its sole match; quick open's matching is glob/substring (§4.2), so a unique match's query is often *not* a prefix of its path at all (e.g. `match` uniquely matching `internal/match/match.go` starts mid-path) — `Remainder` deliberately reports `ok=false` rather than inventing a remainder from wherever the substring happened to occur, so ghost text simply doesn't appear in that case; the secondary highlight still applies regardless, since it depends only on match count. Scrolling the sole match into view needed no new code in either view — both already clamp their scroll position around the currently-selected row every frame, and the sole match is always that selected row by construction. Covered by `TestRemainderReturnsLiteralContinuation`/`TestRemainderCaseInsensitive`/`TestRemainderRejectsMidStringMatch`/`TestRemainderRejectsQueryLongerThanCandidate`/`TestRemainderEmptyQueryReturnsWholeCandidate` (`internal/match/match_test.go`), `TestQuickOpenDrawShowsGhostTextForPrefixMatch`/`TestQuickOpenDrawSuppressesGhostTextForMidPathMatch` (`internal/ui/views/quickopen_test.go`), and `TestBrowserJumpDrawShowsGhostTextForSoleMatch` (`internal/ui/views/browser_test.go`).

**`?` help overlay listing every currently-live keybinding (spec §5.4)**: a small bordered popup (`internal/ui/help.go`), title `keys` embedded in its border the same way the open-files popup's own title is, anchored flush to the screen's upper-right corner immediately below whatever title bar row(s) the current context occupies. Content is grouped and ordered to match what's actually on screen — the main (topmost) title bar's legend first, then any secondary title bar's (the primary preview view's file title bar and/or goto-line prompt row, the open-files popup's own header), recomputed fresh every frame from a new `App.helpLegendGroups`/`helpTitleRows` pair rather than snapshotting the context it was opened from. `?` is otherwise unused throughout the app; it toggles the overlay everywhere except a live query/find text-input context (quick open, content search, jump to file, in-file find's prompt) — `?` is also a quick open glob-wildcard character (§4.1), so those contexts must keep treating it as literal input, gated by a new `App.textInputActive`. While open, every title bar's own legend is suppressed — left-hand content (path, mode label, query text) stays, only the keybinding portion disappears — except the main title bar, which shows a single replacement entry, `[?] hide keys` (new `canvas.HideKeysLegend`), so the way to close the overlay is always visible. This required a small accessor on each view (`Browser.CurrentLegend`, `QuickOpen.CurrentLegend`, `Search.CurrentLegend`, `OpenFiles.CurrentLegend`, `Preview.CurrentFileLegend`, `Preview.GotoPromptLegend`) exactly mirroring the same state precedence its own `Draw` already uses to pick a legend, plus a new `Shared.HelpVisible` field so every view's `Draw` can suppress its own legend without a signature change. Covered across `internal/ui/help_test.go` and `internal/ui/views/*_test.go` (toggle/suppression gating, legend-accessor precedence, group ordering including the three-simultaneous-title-bar edge case, and an end-to-end render check of the box's position/content via `tcell.SimulationScreen`); hand-verified in a real terminal (tmux) across every view and overlay, including narrow-terminal clamping at the minimum enforced size.

**Main title bar's legend fully dims while the open-files overlay is active (spec §2.3, §5.2)**: the primary preview view's own header row is still drawn underneath the open-files-list popup (§2.3), but until this fix it kept showing its full `previewLegend` — switch files, quick open, browse, search, quit — at normal weight even though the overlay owns every key while it's open, and none of those five actions are actually reachable until it's closed (confirmed against `OpenFiles.HandleKey`, which handles only paging/selection/Enter/digit-select/`x`/Escape — Tab included). A new `canvas.DrawHeaderAllDimmed` generalizes the existing single-entry `DrawHeaderDimmed` (added for the "switch files" empty-list case) to dim an entire legend at once — everything after the left-hand root path renders in `StyleHeaderDim`, the root path itself untouched — and `drawPreviewHeader` (`internal/ui/render.go`) now calls it whenever `a.overlay == views.OverlayOpenFiles`, checked ahead of the existing empty-list single-entry dim (which no longer applies in that overlay: there's no "switch files" entry left undimmed to single out once the whole row already is). Covered by `TestDrawHeaderAllDimmedStylesEverythingAfterLeft` and `TestDrawHeaderAllDimmedStylesWholeRowWhenLeftDropped` (the canvas primitive, `internal/ui/canvas/canvas_test.go`) and `TestDrawPreviewHeaderDimsWholeLegendWhenOpenFilesOverlayActive` (the overlay-gating itself, `internal/ui/render_test.go`), all via `tcell.SimulationScreen` cell-style inspection like the existing dim tests.

**Hold-to-quit gesture (spec §5.2, §7)**: a single `q` tap could previously discard the entire session's open-files state instantly, with no confirmation and no visual warning; `q` now must be held continuously for a fixed duration (1 second) before the app actually quits. `views.Preview.HandleKey`'s `ActionQuit` return value was renamed `ActionQuitKey` and its meaning changed from "quit now" to "`q` was pressed this frame, progress the hold" — quitting itself moved out of the primary-view key handler and into `App`'s own timer, since only the dispatcher (via the periodic resize-poll ticker, `internal/ui/app.go`) can track elapsed hold time across frames and infer a release. Three new `App` methods carry the timing: `progressQuitHold` starts the hold on the first `q` event and quits once `quitHoldDuration` has elapsed since; `resetQuitHold` cancels an in-progress hold (called whenever `handleKey`'s primary-view branch sees any action other than `ActionQuitKey`); `checkQuitHoldRelease`, called every resize-poll tick regardless of overlay, infers a release from a gap in `q`'s key-repeat stream exceeding `quitHoldReleaseGap` (600ms — terminals deliver no key-up event, so a held key can only be inferred from a steady stream of repeats). While a hold is in progress, `drawPreviewHeader` (`internal/ui/render.go`) replaces the header/title bar's usual root-path-plus-legend content with a new `canvas.StyleHeaderQuit` (saturated red, the one deliberate exception to §5.3's "subtle, not flashy" default) full-row style and a right-aligned "quitting..." message, fading away left-to-right via a new `canvas.DrawHeaderQuitting` primitive and `internal/toast.Decide` (reused with a zero display duration so the fade starts immediately, `messageLen` set to the row's full width rather than a short string's rune count) so the row's right edge stays anchored while its left edge erodes in step with the hold's remaining time; releasing at any point, including mid-fade, aborts the gesture with no partial credit. The legend's quit entry was reworded from `[q] quit` to `[hold q] quit` (`previewLegend`, `internal/ui/render.go`) to advertise the new behavior directly. `progressQuitHold`/`resetQuitHold`/`checkQuitHoldRelease`'s timing logic is unit-tested with synthetic backdated timestamps rather than real sleeps (`internal/ui/app_test.go`), `Preview.HandleKey`'s `ActionQuitKey` return is covered in `internal/ui/views/preview_test.go`, and the fading header itself is covered via `tcell.SimulationScreen` at the hold's start/midpoint/end (`TestDrawQuitHoldHeaderFadesLeftToRight`, `internal/ui/render_test.go`); the actual felt timing against real OS/terminal key-repeat behavior (as opposed to the synthetic-duration unit tests) still needs manual confirmation in a real terminal.

**Hold-to-quit follow-up: near-instant release detection via a deadline timer, adaptive to the terminal's own repeat cadence, plus a deferred-exit grace window (spec §5.2)**: manual testing against real OS/terminal key-repeat behavior surfaced two problems the synthetic-timing unit tests couldn't catch, and a first pass at fixing the first one (a single tightened-but-still-fixed release-gap constant, checked only on the existing resize-poll ticker) still wasn't responsive enough on a second round of manual testing — releasing `q` visibly took several redraw frames to register. The fix that actually lands: releases are now detected via a dedicated, per-hold `time.Timer` (`App.quitHoldTimer`) reset to fire exactly `quitHoldReleaseThreshold()` after every `q` event, with `Run`'s event loop selecting on its channel directly — this removes the resize-poll ticker's own interval (up to `resizePollInterval`, 100ms) from the detection latency entirely, since a release now registers the instant the threshold itself elapses rather than on whatever's left of the next coarse tick. `quitHoldReleaseThreshold` is also now adaptive rather than a fixed guess: for the first two `q` events (`quitHoldRepeats < 3`) it's still the generous, fixed `quitHoldReleaseGapInitial` (600ms — the interval between the first and second event reflects the OS's *initial* repeat delay, not its steady repeat rate, so it can't be trusted as a baseline), but from the third event on it tracks `quitHoldReleaseMultiplier` (2x) times `App.quitHoldLastInterval` (the most recently observed inter-repeat gap), clamped to `[quitHoldReleaseGapFloor, quitHoldReleaseGapCeiling]` (40ms/250ms) — this tracks whatever the current terminal's actual auto-repeat rate happens to be instead of a single constant that's either too sluggish on a slow-repeating terminal or too trigger-happy on a fast one. Separately, reaching the end of the hold used to quit the process the instant `quitHoldDuration` elapsed, but the terminal's key-repeat stream runs on its own clock independent of that timer — `q` could still be physically down (and the terminal still about to deliver more repeat events for it) at that exact instant, and those events would then fall through to the shell once dirtree's process exited and the terminal returned to normal input mode, typing stray `q` characters at the prompt. `progressQuitHold` now sets a new `App.quitConfirmed` flag instead of quitting outright once the duration elapses; `checkQuitHoldRelease` only actually sets `App.quit` once a release is subsequently detected on a confirmed hold; and `handleKey`'s primary-view branch, once confirmed, stops forwarding keys to `Preview.HandleKey` entirely — every key besides `q` is ignored outright, and `q` itself only keeps bumping the release-tracking timestamp (and rescheduling the timer) — so nothing can act mid-quit and any trailing repeats are drained by the app's own loop rather than leaking out. Covered by `TestProgressQuitHoldConfirmsOnceDurationElapsedWithoutQuitting`, `TestCheckQuitHoldReleaseQuitsOnceConfirmedAndReleased`, `TestCheckQuitHoldReleaseDoesNotQuitConfirmedHoldStillRepeating`, `TestQuitHoldReleaseThresholdIsFixedForFirstTwoEvents`, `TestQuitHoldReleaseThresholdAdaptsOnceRepeating`, `TestQuitHoldReleaseThresholdClampsToFloor`, `TestQuitHoldReleaseThresholdClampsToCeiling`, `TestCheckQuitHoldReleaseUsesAdaptiveGapOnceRepeating`, `TestCheckQuitHoldReleaseKeepsHoldWithinAdaptiveGap`, and `TestHandleKeyIgnoresOtherKeysOnceConfirmed` (all `internal/ui/app_test.go`, synthetic timestamps/durations per §5.3's testing discipline — the deadline-timer wiring itself is exercised indirectly by these since `progressQuitHold` always (re)arms it, but its firing in `Run`'s actual select loop is rendering-layer plumbing, not unit-tested); the felt improvement against real key-repeat behavior — snappier release, no stray `q`s left in the shell — still needs a fresh manual pass to confirm.
